{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        {
          "src": "package.json",
          "dest": "package.json"
        },
        "npm install"
      ]
    }
  },
  "deploy": {
    "startCommand": "npm run start"
  }
}
